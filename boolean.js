"use strict";
let isDone = false;
isDone = true;
console.log(typeof isDone); // boolean
let isOk = true;
let isNotOk = new Boolean(true); // when you use the new keyword, it creates a new object instead of a primitive value
