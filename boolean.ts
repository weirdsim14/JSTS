let isDone: boolean = false;
isDone = true;

console.log(typeof isDone); // boolean

let isOk: Boolean = true;
let isNotOk: boolean = new Boolean(true); // when you use the new keyword, it creates a new object instead of a primitive value